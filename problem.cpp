#include <bits/stdc++.h>
#include <string.h>
#include <cctype>
#include <map> 
using namespace std;


void solve()
{
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int s, n, x, y;
    cin >> s >> n;
    
    while (n--)
    {
        cin >> x >> y;
       solve(s, n, x, y);
        // cout << solution;
    }

    return 0;
}

